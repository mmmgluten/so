import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.AngularTest.app',
  appName: 'Angular Test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
